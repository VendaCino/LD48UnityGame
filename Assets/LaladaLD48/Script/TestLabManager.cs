using UnityEngine;

namespace LaladaLD48.Script
{
    public class TestLabManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
